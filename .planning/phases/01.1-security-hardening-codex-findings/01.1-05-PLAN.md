---
phase: 01.1
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - govori.py
autonomous: true
requirements: [REL-01]
executor: claude

must_haves:
  truths:
    - "stop_and_transcribe (govori.py:1610-1630): defensive guard перед `np.concatenate(audio_chunks, ...)` — если audio_chunks пустой к этому моменту, set_hud(False) и return без exception"
    - "Guard работает и в случае race с другим thread, который мог reset audio_chunks между check на line 1619 (внутри lock) и concatenate на line 1630 (вне lock)"
    - "_record_until_enter (govori.py:2700, CLI voice amend в `govori notes`): try/except PortAudioError на InputStream creation/start; на ошибку — печать понятного сообщения и graceful return (не crash CLI)"
    - "Сообщение об ошибке consistent с daemon recording mic-error path (REL-01)"
    - "Не введены новые exception swallowing — non-PortAudioError exceptions остаются пробрасываемыми"
---

<objective>
Fix HIGH (новый bonus bug) + MEDIUM REL-01 (Codex finding): empty audio_chunks crash и mic-handling в CLI voice amend.

**Two issues:**

1. **HIGH — Empty audio_chunks crash в stop_and_transcribe:** Подтверждено exception в `~/Library/Logs/govori.err.log`:
   ```
   File "govori.py", line 1630, in stop_and_transcribe
       audio = np.concatenate(audio_chunks, axis=0).flatten()
   ValueError: need at least one array to concatenate
   ```
   Race: line 1619 проверяет `if not audio_chunks` ВНУТРИ `_state_lock`, но line 1623+ работает с глобальным `audio_chunks` ВНЕ lock. Между check и concatenate другой thread может пересобрать state. Defensive: либо re-check под lock'ом перед concatenate, либо обернуть concatenate в try/except.

2. **MEDIUM — CLI mic handling в `_record_until_enter` (govori.py:2700):** `sd.InputStream` создаётся без try/except PortAudioError, в отличие от daemon recording path. Если мик отсутствует/denied при `govori notes` voice amend — process crash, не graceful failure.

**Что делать:**
1. govori.py:1630 — добавить guard:
   ```python
   if not audio_chunks:
       set_hud(False)
       return
   audio = np.concatenate(audio_chunks, axis=0).flatten()
   ```
   Желательно re-acquire `_state_lock` для local copy: `with _state_lock: chunks_snapshot = list(audio_chunks)`, потом работать с `chunks_snapshot`. Это устраняет race по ссылке.
2. govori.py:2700 — найти `_record_until_enter`, обернуть InputStream creation/start в try/except PortAudioError; на error печатать сообщение типа `"! No microphone available — voice amend canceled"` и return (не raise, не sys.exit).
3. Симметрично с daemon recording mic-error pattern (см. как REL-01 реализовано там).

**Не делать:**
- Не делать большой refactor concurrency (это вне scope, идёт в фазу 02)
- Не трогать pyaudio/sounddevice инициализацию глобально

Output: govori.py с defensive empty-check + CLI mic handling. Атомарный коммит `fix(01.1-05): empty audio_chunks guard + CLI mic error handling (REL-01)`.
</objective>
