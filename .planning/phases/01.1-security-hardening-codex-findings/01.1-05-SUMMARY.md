---
phase: 01.1
plan: 05
status: complete
executor: claude
---

## Changes

**govori.py** (+21 −6):

1. **`stop_and_transcribe` (line 1681-1707):** Snapshot `audio_chunks` to local `chunks_snapshot` INSIDE `_state_lock`. All downstream code (`sum`, debug print, `np.concatenate`) operates on the snapshot. Eliminates race where another thread (cancel_recording, health-monitor) reassigns `audio_chunks=[]` between the in-lock guard and the out-of-lock concatenate.

2. **`_record_until_enter` (line 2781-2800):** Wrap `sd.InputStream(...).start()` in try/except `sd.PortAudioError`. Distinguish denied vs no-mic, print actionable message, return None. Mirrors the daemon recording mic-handling pattern at line 1446-1460. Caller (`_voice_amend`) already handles `audio is None` gracefully.

## Findings closed
- HIGH (bonus, from err.log): `np.concatenate` ValueError on empty audio_chunks race
- MEDIUM REL-01: `_record_until_enter` crashed on missing/denied mic in CLI voice amend

## Verification
- `python3 -c "import ast; ast.parse(...)"` — OK syntax
- Lock-discipline: chunks_snapshot is a list of numpy array references — refs themselves immutable, no race on contents
- CLI mic try/except scope: covers stream creation + start, where PortAudioError raises

## Commit
`fix(01.1-05): empty audio_chunks guard + CLI mic error handling (REL-01)`
