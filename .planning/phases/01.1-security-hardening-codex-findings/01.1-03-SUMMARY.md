---
phase: 01.1
plan: 03
type: summary
executor: codex
date: 2026-05-05
commit: TBD
---

## Changes

- Added `_health_monitor_owns_hud` ownership tracking for the accessibility-revoked fatal HUD state.
- Updated `_tap_health_check()` to call `cancel_recording(skip_hud=True, quiet=True)` before showing the accessibility-revoked fatal HUD.
- Updated tap recovery to hide the HUD only when the health monitor still owns it and `_hud_error_mode == "error_fatal"`.

## Findings Closed

- SEC-02 HIGH: revoked Accessibility now cancels active recording and closes the microphone stream without waiting for fn-up.
- SEC-02 MEDIUM: tap recovery no longer unconditionally hides unrelated HUD states.

## Verification

- `python3 -c "import ast; ast.parse(open('/Users/genlorem/Projects/govori/govori.py').read())"`

## Commit Hash

- TBD
