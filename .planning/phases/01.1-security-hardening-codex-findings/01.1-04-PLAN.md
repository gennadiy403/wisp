---
phase: 01.1
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - govori.py
autonomous: true
requirements: [SEC-04]
executor: claude

must_haves:
  truths:
    - "Privacy notice (en, govori.py:305-312) НЕ содержит ложь 'Audio is not stored'. Вместо: явное упоминание что note-mode сохраняет .opus в ~/life/state/govori-audio/ для recall, с ретеншн-политикой (или 'no automatic deletion' если её нет)"
    - "Privacy notice (ru, govori.py:385-392) — параллельные изменения, корректный русский, без mojibake"
    - "Notice упоминает 3 cloud-пути: Whisper (audio→text), Claude (note classification), OpenAI chat completions (predict/rephrase) с упоминанием configurable base_url"
    - "Текст укладывается в существующий visual-стиль (cyan headers, dim grey body, ANSI codes)"
    - "Step 2/4 numbering сохранён, step header не сдвинут"
---

<objective>
Fix MEDIUM SEC-04 + LOW finding: privacy notice врёт про audio storage и не упоминает predict/rephrase API path.

**Что делать:**
1. Прочитать текущий en notice (`govori.py:305-312`) и ru (`govori.py:385-392`)
2. Заменить ложное "Audio is not stored after transcription" на правдивое описание:
   - Voice audio → OpenAI Whisper (only for transcription)
   - В note-mode: encoded as Opus и сохраняется локально в `~/life/state/govori-audio/YYYY-MM-DD/HHMMSS_Ns.opus` для recall (audio quality 32kbps, ~14 MB/hour). Не загружается дополнительно никуда, retention manual
   - Note text → Anthropic Claude (classification only, not retained by Anthropic per their TOS)
   - Predict/rephrase mode (если используется): transcribed text → OpenAI chat completions (configurable base_url, может быть локальным)
3. Параллельно обновить ru-версию (perevod корректный, проверить Cyrillic)
4. Сохранить визуальный стиль (\033[1m, \033[2m, \033[36m коды)
5. Если notice стал длиннее — убедиться что step header (Step 2/4) и flow не ломается визуально

**Не делать:**
- Не добавлять cli prompt "do you accept" (notice остаётся informational, без consent flow — это вне scope)
- Не менять onboarding step нумерацию
- Не трогать другие notice-like строки

Output: govori.py с честным privacy notice. Атомарный коммит `fix(01.1-04): correct privacy notice — disclose audio retention + predict API (SEC-04)`.
</objective>
