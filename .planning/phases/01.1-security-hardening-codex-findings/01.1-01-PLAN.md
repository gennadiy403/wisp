---
phase: 01.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - govori.py
autonomous: true
requirements: [SEC-03]
executor: claude

must_haves:
  truths:
    - "OpenAI client `timeout` параметр = 30.0 (вместо 5.0) в обеих ветках инициализации (с base_url и без)"
    - "`_encode_and_transcribe` различает permanent errors (4xx кроме 408/429) от transient (timeout/connection/5xx/408/429): возвращает не-None при non-retryable, чтобы caller не ставил их в retry buffer"
    - "Caller-логика в `stop_and_transcribe` и `_note_pipeline_background` не пишет в _retry_buffer для permanent errors"
    - "Per-request timeout policy в `_timeout_for_duration` обновлена под новый верх: минимум 30s или duration-based, что больше"
    - "Manual quick test: фиксированный timeout=30 не ломает обычные транскрипции"
---

<objective>
Fix SEC-03 finding: заявленный 30s timeout не реализован (хардкод 5.0), и permanent API errors ретраятся как transient.

**Что делать:**
1. govori.py:870-873 — заменить `timeout=5.0` на `timeout=30.0` в обеих веточках OpenAI клиента
2. Найти и обновить `_timeout_for_duration` если оно ограничивает сверху <30s
3. govori.py:~1468 — refactor `_encode_and_transcribe`: для `APIStatusError` определить retryability по status code (408/429/5xx → retryable; 4xx остальные → permanent), вернуть структурированный результат (либо отдельный sentinel, либо raise/return tuple)
4. Обновить callers в `stop_and_transcribe` и `_note_pipeline_background` чтобы не класть permanent errors в `_retry_buffer` (показывать `error_fatal` вместо `error_retryable`)
5. Сообщение "Transcription timed out (Xs)" должно использовать актуальное значение

**Не делать:**
- Не менять retry-логику (это plan 01.1-02)
- Не трогать health-monitor (plan 01.1-03)

Output: govori.py с правильным 30s timeout, классификация permanent vs transient errors, корректный routing в HUD modes. Один атомарный коммит `fix(01.1-01): API timeout 30s + permanent error classification (SEC-03)`.
</objective>
