---
phase: 01.1
plan: 01
status: complete
executor: claude
---

## Changes

**govori.py** (+41 −14):
- `client` (line 870-873): default `timeout=5.0` → `timeout=30.0` (affects predict/rephrase chat completions which don't override per-request)
- `_timeout_for_duration` (line 1422-1430): floor lifted from 5-8s to 30-60s scaled by audio duration
- `_encode_and_transcribe` (line 1437): default arg `timeout=5.0` → `timeout=30.0`; updated docstring
- `_encode_and_transcribe` `APIStatusError` handler (line 1471-1478): split into transient (5xx + 408 + 429 → return None) vs permanent (other 4xx → return PERMANENT_API_ERROR)
- New module-level sentinel `PERMANENT_API_ERROR = object()` near retry state (line 909-911)
- `_transcribe_with_auto_retries` (line 1518-1519): break loop on PERMANENT_API_ERROR, propagate to caller
- `stop_and_transcribe` caller (line 1675-1681): if PERMANENT_API_ERROR — show `error_fatal` HUD, no retry buffer
- `_note_pipeline_background` caller (line 1597-1599): same — fatal HUD instead of retry
- `_retry_transcription` (line 1252-1257): if retry returns PERMANENT_API_ERROR — drop buffer, show fatal
- CLI voice amend (`govori notes`) (line 2837): handle PERMANENT_API_ERROR alongside None / empty

## Findings closed
- HIGH SEC-03 timeout=5.0 hardcode (verified in prod logs)
- MEDIUM SEC-03 permanent errors retried as transient

## Verification
- `python3 -c "import ast; ast.parse(...)"` — OK syntax
- No remaining `timeout=5.` matches in code
- No regressions to retry mechanic (covered by plan 01.1-02 separately)

## Commit
`fix(01.1-01): API timeout 30s + permanent error classification (SEC-03)`
