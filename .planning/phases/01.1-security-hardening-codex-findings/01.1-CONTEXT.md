# Phase 1.1: Security Hardening — Codex Review Findings

**Created:** 2026-05-05
**Status:** Ready for execution
**Source:** Cross-AI review (Codex gpt-5.5/high) of phase 01-security-safety implementation
**Type:** INSERTED (decimal phase between 01 and 02)

## Background

После завершения фазы 01-security-safety (5 заявленных мер: SEC-01..04, REL-01) был выполнен независимый security-review через Codex CLI. Codex нашёл 9 реальных проблем в реализации, плюс runtime-логи govori (`~/Library/Logs/govori.{out,err}.log`) подтвердили один дополнительный bug. Все находки валидированы по коду перед созданием этой фазы.

Полный отчёт ревью: `01-CODEX-REVIEW.md` в фазе 01-security-safety.

## Findings to Fix

| # | Severity | File:line | Mера | Issue | Plan |
|---|---|---|---|---|---|
| 1 | HIGH | govori.py:870 | SEC-03 | `timeout=5.0` хардкод вместо заявленных 30s — taymouts срабатывают на нормальных транскрипциях, видны в проде | 01.1-01 |
| 2 | HIGH | govori.py:1146-1157 | SEC-03 | Retry без single-flight lock — повторные клики стартуют parallel `_retry_transcription` threads на одном буфере | 01.1-02 |
| 3 | HIGH | govori.py:2497-2526 | SEC-02 | При revoked Accessibility health-monitor показывает HUD, но не отменяет recording/audio_stream — мик может писать бесконечно | 01.1-03 |
| 4 | HIGH | govori.py:1630 | (bonus) | `np.concatenate(audio_chunks, ...)` падает с `ValueError: need at least one array to concatenate` — race вне `_state_lock` (подтверждено exception в err.log) | 01.1-05 |
| 5 | MEDIUM | govori.py:1468 | SEC-03 | Permanent `APIStatusError` (400/401/403) ретраятся как transient через `None` return | 01.1-01 |
| 6 | MEDIUM | govori.py:1259 | SEC-03 | Retry success всегда `paste_text()` — теряет note_mode/predict/auto-send контекст | 01.1-02 |
| 7 | MEDIUM | govori.py:2521-2524 | SEC-02 | Recovery `set_hud(False)` без проверки ownership — может скрыть unrelated active HUD | 01.1-03 |
| 8 | MEDIUM | govori.py:305-312 + 385-392 | SEC-04 | Privacy notice врёт: "Audio is not stored" — а note-mode персистит .opus в `~/life/state/govori-audio/` | 01.1-04 |
| 9 | MEDIUM | govori.py:2700 | REL-01 | `_record_until_enter()` (CLI voice amend) не ловит PortAudioError — крашится вместо REL-01 failure path | 01.1-05 |
| 10 | LOW | govori.py:2252 | SEC-04 | Privacy notice не упоминает predict/rephrase API path (chat completions) | 01.1-04 |

SEC-01 (shell injection fix) — clean, без находок.

## Plan Breakdown

5 планов, исполняются последовательно (single-file проект, не нужна параллельность):

- **01.1-01** (Claude): API timeout 5s → 30s + classify permanent vs transient API errors
- **01.1-02** (Codex via `codex exec`): retry single-flight lock + mode-aware retry dispatch
- **01.1-03** (Codex via `codex exec`): health-monitor cancel recording + ownership tracking
- **01.1-04** (Claude): privacy notice corrections en+ru (note-mode storage + predict path disclosure)
- **01.1-05** (Claude): empty audio_chunks guard + CLI mic error handling

Codex используется для двух плана (02 и 03) с race conditions — symmetric paranoia: модель которая нашла проблему пишет фикс, другая модель (Claude) ревьюит обратно.

## Out of Scope

- Архитектурные изменения (это фаза 02 в roadmap)
- Новые security threats не из этого ревью
- Refactor phase 01 кода вне найденных issue

## Verification

После всех 5 планов — повторный Codex security-review этой же фазы (с теми же критериями) для подтверждения, что все 9 находок закрыты и не введены регрессии.
